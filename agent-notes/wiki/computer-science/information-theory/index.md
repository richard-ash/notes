# Computer Science > Information Theory

Entropy, coding, and compression — the mathematics of representing information in the fewest bits, and its equivalence to prediction.

## Articles

- [[lossless-compression]] — Sexton's "compression is prediction" explainer: the transform/model/entropy-coder anatomy, arithmetic coding as one binary fraction, Huffman's whole-bit rounding cost, Shannon entropy as the −log₂p floor that is always relative to a model, order-1 context halving output, the DeepMind language-modeling-is-compression equivalence (cross-entropy loss is literally bits per token; GPT-2 squeezes Dickens to 10% vs order-1's 24%), and why shared-model size, compute, and bit-exact determinism keep gzip/Brotli in HTTP
