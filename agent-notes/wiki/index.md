# wiki/

Compiled articles, organized by domain. Each domain directory has its own `index.md` listing one-line summaries of every article within it.

Domain taxonomy mirrors the personal vault: `computer-science/`, `biology/`, `engineering/`, `food/`, `healthcare/`, `companies/`, etc. Domains are created lazily on first ingest.
